spring:
  application:
    name: myfitnessapp
  datasource:
    url: jdbc:mysql://localhost:3306/mi-huella-pruebas
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
server:
  port: 80